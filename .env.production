# Используется при `npm run build` (продакшен).
# Относительный /api — фронтенд ходит на тот же домен (https://auto.minestill.fun/api),
# nginx проксирует /api на бэкенд (127.0.0.1:4245).
VITE_API_URL=/api
VITE_TELEGRAM_BOT_NAME=FindAutoBot
VITE_APP_ENV=production
